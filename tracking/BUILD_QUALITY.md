# BUILD_QUALITY.md — "does it run, is it structured, would you trust it"

Kept truthful. Update whenever a check is actually re-run — don't carry forward a stale ✅.

---

## Cold start
- Backend API: ✅ `uvicorn api.main:app` starts clean and serves real data — verified 2026-08-26 (`curl /health` → `{"status":"ok","invoices_loaded":60,"reserves_active":2}`)
- **Full 2-command cold start, genuinely re-verified 2026-08-27 from a fresh `git clone` (not the working dir):** `setup.ps1` then `run.ps1`. First attempt (cloned into a deeply nested path) caught a real bug — `pip install` failed on a Windows `MAX_PATH` (260-char) error installing one of `anthropic`'s beta type files, but `setup.ps1` never checked pip's exit code and printed "Setup complete" anyway; a judge would only discover the broken venv later, on a confusing `ModuleNotFoundError`. Fixed: `setup.ps1` now asserts `$LASTEXITCODE` after venv creation, `pip install`, and `npm install`, and fails loudly naming the failed step; `run.ps1` now checks the background API process didn't die immediately. Re-cloned into a short path (`C:\Users\amogh\pk_test`) and re-ran clean: venv created, all Python deps installed (including `anthropic`/`twilio`), dashboard deps installed via `npm install`, **564/564 tests pass**, `uvicorn api.main:app` serves `GET /docs` → `200`, `npm run dev` serves the dashboard root → `200`. Test artifacts and the temp clone were deleted after verification. Fix committed as `cc8608a`, pushed. `setup.sh`/`run.sh` mirror the same steps and already fail loud via `set -euo pipefail` (bash treats a non-zero `pip`/`npm` exit as fatal by default, unlike the PowerShell native-exe case) — not executed on a real Unix box this session, no Mac/Linux machine available, but the logic is structurally the same and simpler (no separate exit-code assertion needed).
- Target commands (per README §, to be finalized Day 9/Step 7): `setup.ps1` / `setup.sh` then `run.ps1` / `run.sh` — see repo root.

## Tests
- Test count passing: **564 / 564** at last full run (2026-08-27, after `eval/run_arms.py` + `tests/test_run_arms.py`, the three-arm comparison runner — +10 tests over the 554 below), re-confirmed green in a genuine fresh clone (see "Cold start" above).
- Test count before the three-arm runner: **554 / 554** at last full run (2026-08-27, after packets P16 + P17 — real phone call via Twilio, real Telegram text/audio, real contact-book Telegram linking). New this window: `tests/test_telephony.py` (16 — the `_should_go_real_telephony` gate's four conditions each isolated, dispatch actually calling `place_call`/`send_whatsapp` only when gated, a Twilio failure degrading cleanly and being audited, two credential shapes (Auth Token vs API Key+Secret) both recognized, and the full seeded 45-day run proven to make zero real Twilio calls even with the opt-in flag on) and `tests/test_telegram_dispatch.py` (11 — the mirrored `_should_go_real_telegram` gate incl. the chat-id-required fourth condition, real dispatch on both message and voice paths, a send failure degrading cleanly, the same network-free 45-day-run proof, and **the one real regression this session found and fixed**: `test_remind_now_surfaces_the_real_telegram_send_in_its_response`, which fails against the pre-fix `api/main.py` and passes after — a real Telegram send was succeeding at the dispatch layer while the API response silently omitted it; full story in BUILD_LOG 2026-08-27). Both new files, like every real-provider test file before them, mock the provider module directly — no test in either makes a real network call. Seeded 45-day run re-verified byte-identical across two fresh processes; the run's own numbers (₹23,31,496 etc.) are untouched, since no autonomous action can ever reach a real send regardless of any `.env` credential. Dashboard unaffected (this window's work was backend/infra only) — not re-verified this window, no dashboard files changed.
- **New non-Python dependency this window: Tailscale** (installed via `winget`, used for `tailscale funnel` — exposes the local dev backend at a real public HTTPS URL so Twilio's Voice API, which will not accept inline TwiML on a trial account, can fetch this project's own `/telephony/twiml` webhook). This is a **development/testing tool**, not part of the shipped application — a real deployment (Render, etc.) gets a public URL for free and would not need it. Documented here so "does it run on a stranger's machine" stays honestly answered: the core app needs no tunnel; only a live real-phone-call demo from an undeployed dev machine does.
- Test count before P16/P17: **525 / 525** at last full run (2026-08-27, after packet P15 — real contact identity, name + phone). P15 added **39** net: a new `tests/test_contacts.py` (36 — `Contact`/`ContactBook` validation in isolation including the phone-regex parametrized valid/invalid tables, `WorldRunner._contact_key`/`resolve_contact` scoping and byte-identical-fallback proof, sibling-invoice contact sharing and no-leak-to-other-debtors, the explicit "message needed zero new dispatch code" proof (real Messenger-queue entry, entity's own thread channel, standard `ESCALATION_TEXT` content), every dispatch point — manual sms/message and autonomous voice — carrying the resolved contact additively without changing spoken/written text, the real-Razorpay-call customer block reading `resolve_contact()`, and the full API surface: `POST /entities/{id}/contact` success + `also_applies_to` + 404/422, `GET /contacts`, `GET /entities` carrying `contact`, and `create-mandate-now`'s explicit-override-still-wins / real-contact-as-middle-rung / demo-fallback-when-nothing-submitted three-way fallback chain); plus **3** in `tests/test_reminders.py` (`test_message_is_now_a_real_manual_reminder_channel`, `test_message_reaches_identical_verdicts_as_voice_and_sms_as_a_manual_channel` — the pairwise bound-equivalence proof for the third manual channel, mirroring P14's own `sms`-vs-`message` proof — and `test_remind_now_sends_a_real_message_and_lists_it_back` at the API layer). **Three pre-existing P14 tests needed real changes, not just format**, because their assertion was literally "message is refused as a channel" — now true only for a channel that was never valid to begin with (`"email"`); see `tracking/BUILD_LOG.md` (2026-08-27) for the full accounting, including the one thing the packet's own prediction got right (`_dispatch`'s message branch needed zero new code) and the one thing it didn't predict (three tests needed editing). Seeded 45-day run re-verified unchanged: **₹23,31,496; 23 KEPT / 27 HUMAN_HANDOFF / 12 DISPUTED; `bound_violations()` empty; two fresh runners byte-identical** (audit trail, funnel, and `reminders` list all compared equal). Dashboard `npm run build` and `npm run lint` both clean at the same date.
- Test count before P15: **486 / 486** at last full run (2026-08-26, after packet P14). P14 added **31**, all in the new `tests/test_reminders.py`: the two schema-literal extensions, the sms bound suite (per-debtor cap, all four terminal states, legal stage, plus a 2,000-input equivalence proof that `check_bounds` returns the identical verdict *and* reason for `sms` as for `message` — the test that would catch "added to some enumerations, missed from one"), the `manual_reminder` gate suite (blocked once the ordinary ladder spent the budget, blocked on a paused thread, blocked from a terminal state, 404/422 refusals, custom text as content-only), the TTS suite with `gtts.gTTS` stubbed in both directions (deterministic filename, Hindi lang, typed failure leaving no half-written file, zero-byte guard, empty-text refusal), the runner suite (ESCALATE_2 still emits `voice` and now carries real audio, the same escalation blocked by the shared debtor cap, a TTS outage degrading to a transcript, `PK_REAL_TTS=0` recording `disabled` not `failed`, sms on the sms rail, verbatim custom text, and reminders deliberately not registered as openable instruments), and the API suite driven through `TestClient`. **No existing test was weakened**; adding `sms` to `OUTBOUND_KINDS`/`TouchKind` automatically widened the pre-existing parametrized bound tests and the 1000-walk fuzz in `test_state_machine.py`. Seeded 45-day run re-verified unchanged (₹23,31,496; 23 KEPT / 27 HUMAN_HANDOFF / 12 DISPUTED; `bound_violations()` empty; two fresh runners byte-identical). Dashboard `npm run build` clean at the same date. The count includes packet P13's `tests/test_demo_console.py` (10), which landed in the same window.
- Test count before P14: **445 / 445** (measured at the start of P14, 2026-08-26). Suite stayed offline: the seeded run emits zero `voice` actions (all 4 attempts refused by the touch cap — see BUILD_LOG), so no gTTS network call happens inside `pytest`, and the TTS tests stub `gtts.gTTS` rather than calling it.
- Test count before P12/P13: **438 / 438** (2026-08-26, after packet P11). P11 added **5**, all in `test_integration.py` (31 → 36): the INV-001/Acme Traders regression that reproduces P10's smoking gun (a confirmed mandate must never be recorded as refused two days later), the mirror test that TRUE silence still soft-refuses at 48h (INV-003 — so the fix cannot have weakened `link_timed_out()`), a run-wide check that every reply opens the instruments still inside their window against the exact `action_id` `track_link_sent` used, a check that the only refusals left are ones a debtor actually made, and the pinned 45-day distribution/₹ figure so the headline cannot move without this file moving too. `tests/test_action_layer.py`'s Sentinel unit tests are **untouched** — the bug was in the caller, not in the Sentinel. Verified in both directions: the three fix-specific tests fail against the pre-fix runner and pass after.
- Test count before P11: **433 / 433**. *(The line below recorded 432 for P10; a re-collect during P11 counted 433, so the P10 figure was one short. Corrected here rather than carried forward.)* P10 added **38**: `test_day_story.py` is new (33 — the five read-only Day Story routes, the cart-customer-has-no-name honesty case, the empty-mandate-timeline case, and `test_advance_and_day_story_agree`), `test_state_machine.py` 118 → 123 (the proof that `check_bounds_detailed()` can never disagree with `check_bounds()` over 5000 random inputs, and that `check_bounds()` itself still short-circuits). Dashboard `npm run lint` (oxlint) and `npm run build` (vite) both clean at the same date.
- Test count before P10: **394 / 394** (2026-08-26, after packet P9). P9 added **123**: `test_review_queue.py` is new (18 — the API-level human-in-the-loop suite), `test_ledger.py` 13 → 33 (the confidence gates, the approve/reject/mark-handled clicks, `human_resolution`, the kill-switch), `test_state_machine.py` 42 → 118 (terminal-state immutability re-stated as 4 terminal states × the whole pipeline event vocabulary, plus the one exception pinned from both directions), `test_integration.py` 24 → 31 (holds and the formal-notice draft inside a real 45-day run, the pause control-run comparison, gate determinism, and the id-sequence check that no Action is constructed outside the ledger), `test_api.py` 5 → 7 (route surface + the `POST /events` refusal of `human_resolution`).
- Previous line, kept for the trail: **271 / 271** before P9. P8 added 23 of them (`test_state_machine.py` 35 → 42 with the per-debtor touch-cap violation tests, `test_ledger.py` 6 → 13 with the Action-backed-nudge and shared-budget tests, the new `tests/test_sim_clock.py` = 8, and `test_integration.py` 23 → 24 where the P2 truth-pinning test split into per-debtor cap + shared-budget assertions). The count moved from 217 → 271 across two packets landing in the same window; `tests/test_config.py` (23) is not P8's. P7 contributed 8 of the 271 (`test_ollama_provider.py` 25 → 31 for reference-date injection, shared-assembly dedup and prompt-in-cache-fingerprint; `test_perception.py` 10 → 12 for the Anthropic side of the same shared helper) — re-verified green at 271 on 2026-08-26 after the P7 changes.
- Last full `pytest tests/ -q` run: 2026-08-26 (post-P11), 438 green, ~10s warm
- Suite is fully OFFLINE, re-verified 2026-08-26 in three cache modes (warm `.cache/perception`, cold after `rm -rf .cache/perception`, and `PK_PERCEPTION_CACHE=0`) — 217 green in each. `tests/test_integration.py::test_no_network_is_attempted_by_default` monkeypatches every Razorpay entry point to raise, then runs a full 45-day world: one attempted network call would fail the suite.

## Secrets hygiene
- Grep check for `rzp_` / `sk-ant` in tracked files: ✅ clean — `git grep -nE "rzp_(test|live)_[A-Za-z0-9]|sk-ant-api" -- .` re-run by the lead against the full staged P15 diff at merge time (2026-08-27); only match is this checklist line's own description of the command
- **Re-run 2026-08-27 (P16/P17), widened to cover every new credential type added this window** — Twilio (`AC[0-9a-f]{32}`, `SK[0-9a-f]{32}`), Meta WhatsApp access tokens (`EAAP...`), Telegram bot tokens: ✅ clean across the whole repo, only the pre-existing fake fixtures in `tests/test_razorpay_client.py` match. All real values for these four new credentials live only in the gitignored `.env`, confirmed via `git status`/`git diff` before every commit this window.
- Last check date: 2026-08-27 (full repo, after P16/P17)
- `.env` gitignored: ✅ (`.gitignore` created Phase A setup, 2026-08-26)
- `.env.example` contains placeholders only, no real values: ✅ (verified at creation, 2026-08-26)

## Reproducibility
- Two seeded simulator runs diffed (`python -m sim.run --days 45 --seed 42` × 2): ✅ re-verified 2026-08-26 after the P8 clock fix, byte-identical output (sha256 `e849ecc578ba9b05…` on both, `diff` empty). Output is now **463 events**, not the 408 recorded before: the day-0 beat that `VirtualClock.advance()` had silently been dropping now runs. Determinism is the invariant here; 408 never was.
- Two seeded dataset generator runs diffed (`python -m data.generate` × 2, sha256 of all `data/*.json`): ✅ verified 2026-08-26, identical hashes
- Two seeded **integration-runner** runs diffed (`WorldRunner(seed=42).advance(45)` × 2, in two separate processes, dumping audit trail + event stream + message queue + funnel + every entity state + the per-debtor touch counter + the held-action queue, clarify counters and pause map + **the Sentinel's `link_sent_at` / `link_opened` sets**): ✅ re-verified 2026-08-26 after packet P11's link-open fix, byte-identical (248,754 bytes of JSON, sha256 `a3be35bfea1dffc2…` on both, `diff` empty). *(Earlier P9 figure was 171,863 bytes / `fad45fad02a6fdc1…` over a smaller dump shape; the invariant is `diff` empty, not the byte count.)* The gates are deterministic given the heuristic provider — the same run holds the same actions for the same reasons in the same order, also asserted in-suite by `test_integration.py::test_the_confidence_gates_are_deterministic`. (Earlier P8 figure was 267 KB / `6247e1ea0df3d53d…` over a slightly different dump shape; the invariant is `diff` empty, not the byte count.) Also asserted in-suite by `test_two_fresh_runners_produce_identical_audit_trails`, plus `test_the_same_days_split_differently_land_on_the_same_world` (12 × `advance(1)` == 1 × `advance(12)` — "Advance 1 Day" and "Run to Day 45" cannot diverge).

## BUILD.md acceptance criteria — status
| Day | Criterion | Status |
|---|---|---|
| 1–2 | `sim.run --days 45 --seed 42` replays identically twice | ✅ re-verified 2026-08-26 (post-P8) — byte-identical `diff` across two runs, **463 events** (was 408 before the `VirtualClock` day-0 fix; the count moved because a beat that had never run now runs — determinism is the criterion, not the count), all validate against `engine.schemas.Event` |
| 1–2 | ground_truth covers 100% of messages/invoices/carts | ✅ verified 2026-08-26 — 60/60 invoices, 12/12 carts, 44/44 inbound messages all have ground-truth entries |
| 1–2 | judge-mode read of 10 random conversations agrees labels are fair | ✅ done 2026-08-26 (self, "judge mode" per BUILD.md) — read T-05/T-06/T-16/T-18/T-22 critically, caught and fixed one real mislabel (M-06-4) and a timestamp/narrative inconsistency; logged in BUILD_LOG.md and DECISIONS.md |
| 3 | triage accuracy ≥90% | 🟡 gate scoped to threaded invoices by lead ruling (DECISIONS.md): `heuristic` **91.7% PASS** on the 24 threaded / 71.7% all-60 headline. Ollama measured 2026-08-26: qwen2.5:7b **91.7% threaded** / 53.3% overall; 3b 87.5% / 56.7% — no-thread cases sit under the proven 61.1% info ceiling for every provider. Re-measured unchanged after P7's assembly fix (dates/thread-counts in the triage user turn moved nothing). P7 also TRIED tightening `triage.md` and reverted it: it bought the all-60 headline (53.3% → 65.0%) by spending the gated threaded metric (91.7% → 83.3%), which is metric-shopping — trade-off recorded in BUILD_LOG if the lead ever wants the other side of it. |
| 4 | extraction level accuracy ≥85% | ✅ **PASS on both providers measured** — re-measured 2026-08-26 after packet P7 (reference-date fix + prompt iteration per BUILD.md Day 4's own loop): `heuristic` **97.7% PASS (in-sample caveat)** · qwen2.5:7b **88.6% PASS** (was 77.3% FAIL) · qwen2.5:3b 70.5% FAIL (was 59.1%), all `metrics/extraction_accuracy_ollama_{7b,3b}.json`. Ladder stays at **five levels** — BUILD.md's L2/L4-merge fallback not needed, not taken. Quote the 7b number as "88.6%, gate cleared in all three runs": the same prompt+model at temperature 0 scored 90.9/88.6/88.6 across three runs (±1 message of GPU non-determinism — see BUILD_LOG). |
| 5 | every bound has a violation test that fails correctly | ✅ 8/8 bounds, `tests/test_state_machine.py` (42 tests). Bound #4 is now tested at the scope the law is written in — `test_bound_max_touches_per_week_is_scoped_to_the_DEBTOR_not_the_invoice` plus one violation test per touch kind, so the cap can't be dodged by switching instrument. |
| 5 | dispute from any state → DISPUTED, no further outbound actions | ✅ parametrized over all 11 non-terminal states |
| 5 | 1000 random event sequences all terminate in KEPT/CLEAN_LOSS/HUMAN_HANDOFF | ✅ (DISPUTED included in the terminal set — see state_machine.py docstring). Still green after packet P9 added `human_resolution`, the one event that can move a terminal state: it is **excluded** from the fuzz pool because that pool models event streams the pipeline can actually produce, and nothing in `engine/` emits it while `POST /events` refuses it — both asserted. Excluding it is the stricter choice (it keeps terminal immutability at "immune to every pipeline event" rather than "immune to everything except the event we just added"); the exception is pinned separately from both ends. Full reasoning in DECISIONS.md 2026-08-26 (P9) and in `ALL_EVENT_TYPES`' own docstring. |
| 5 | human-in-the-loop: confidence gates + approval queue (master doc §2.3/§3.6) | ✅ **packet P9, 2026-08-26.** Money gate (<0.90) holds instead of sending; clarify gate (<0.75) asks exactly one question then queues; formal-notice draft reaches the queue with no send path at all; merchant kill-switch pauses a thread everywhere, not just in the UI; `resolve-handoff` closes a handoff both ways. `check_bounds()` re-runs at approve-click time — proven by a hold that goes stale and is refused. 45-day heuristic run: **4 queue items** (3 money-gate holds at conf 0.78 + 1 formal-notice draft), **0 clarify** (the heuristic's discrete confidence table never dips under 0.78 for a promise-level read — stated honestly, not tuned). |
| 6 | real test-mode Payment Link URL in audit trail | ✅ **verified 2026-08-26** — `PK_REAL_RAZORPAY=1` + a fresh `WorldRunner().advance(3)` put a live TEST-mode Payment Link into the ledger's audit trail: entry `AD-00080` on `INV-003`, summary `REAL Razorpay test-mode link: https://rzp.io/rzp/K3z43yQ` (`plink_TU8TBK4FoixASK`, `simulated: false`), and the queued WhatsApp message carries the same URL on rail `wa_native_payment`. Same run also put a real **mandate registration** link in the trail: `AD-00076`, `https://rzp.io/rzp/wQTfueuU` (`inv_TU8TAWNYG0ztkp`) on rail `mandate_link`. Opt-in and rate-limited by design: default (env unset) = zero network calls; at most the first link + first mandate action per run go real (asserted offline by `test_real_razorpay_budget_is_one_link_and_one_mandate_per_run`). |
| 6 | network-kill mid-run → dead-letter, resume works | 🟡 **half-proven against a real failure now.** The first `PK_REAL_RAZORPAY=1` run hit a genuine live 400 from `POST /payment_links` ("Recurring digits in customer contact are disallowed", BUILD_LOG 2026-08-26); the Sentinel retried ×3 with 1/5/15-minute backoff, dead-lettered the action, and the run continued on the simulated rail with nothing lost. Also asserted offline by `test_a_failing_razorpay_call_retries_then_dead_letters_then_falls_back`. Still 🟡: a mid-run *network kill* (vs an API rejection) and an explicit resume path haven't been exercised. |
| 7 | cold start → dashboard funnel with real data in <60s | ✅ **verified 2026-08-27 from a genuine fresh clone** (`setup.ps1` → `run.ps1`), see "Cold start" section above. Two-command path, not a single make-style command, but that's what BUILD.md's own criterion names. |
| 7 | Advance-Day visibly moves money/promises/trust on screen | 🟡 **API level done, dashboard visual pending.** `POST /advance {days}` drives the whole world through the real pipeline (perception → `ledger.process_event` → action layer) and returns `{day, new_events, new_actions, funnel_summary}`; `GET /world` + `GET /funnel` expose clock/provider/counts. `advance(45)` moves 51 active invoices to **23 KEPT / 25 HUMAN_HANDOFF / 3 DISPUTED** (+9 pre-disputed), **₹25,59,383 recovered** (36.6% of active-invoice value), 18 promises kept / 10 broken, trust posteriors off the Beta(2,2) prior, both reserve carts recovered at **0 touches**. Asserted by `test_advance_endpoint_moves_the_funnel` + `test_advance_moves_money_promises_and_trust`. What's left is the dashboard button + screen wiring (P4).<br><br>**These numbers moved down in packet P8 and that is the point.** Before P8 the run showed 29 KEPT / 19 HANDOFF / 3 DISPUTED and ₹40,62,231 (58.2%) — but it got there by contacting some debtors **six** times in a rolling week, because the touch cap was enforced per invoice while CLAUDE.md law 4 and master doc §3.4 both word it per debtor. With the cap counted per debtor the worst window is **2**, 149 touches are blocked and audited over the run, and recovery falls to 36.6%. Nothing was tuned to win the old figure back; the compliant number is the number. Any recovery figure quoted in the README or video must be this one.<br><br>**Packet P9 (2026-08-26) moved them again, and again the smaller number is the honest one.** With master doc §2.3's money gate wired, an extraction under 0.90 confidence no longer sends a mandate offer by itself — it waits for a human. Active-invoice terminals go **23 KEPT / 25 HUMAN_HANDOFF / 3 DISPUTED → 21 / 27 / 3**, and recovery **₹25,59,383 (36.6%) → ₹23,31,496 (33.4%)**, because two mandate offers that used to go out unsupervised now sit in a queue nobody clicked. **When the queue IS worked, it goes the other way:** a merchant who approves each held action once the debtor has touch budget again lands 3 of 3 approvals and finishes at **26 KEPT / 23 HUMAN_HANDOFF / 13 DISPUTED, ₹31,74,725**, with `bound_violations()` still empty. Quote whichever you like, but say which: **₹23,31,496 is the unattended number and is the safe default for the README**; ₹31,74,725 is "with a human working the queue" and must be labelled that way. A merchant who instead clicks Approve the instant an item appears gets **0 of 3** — every click refused by the per-debtor touch cap, because the outreach that elicited the promise already spent the budget (BUILD_LOG 2026-08-26).<br><br>**Packet P11 (2026-08-26) is the first fix that did NOT move these numbers, and that was measured, not assumed.** `Sentinel.mark_link_opened()` had zero call sites, so every dispatched instrument soft-refused itself at 48h regardless of what the debtor did — a confirmed mandate was being recorded as refused two days later. With the call site wired, the 45-day run is **unchanged at 21 KEPT / 27 HUMAN_HANDOFF / 3 DISPUTED, ₹23,31,496 (33.4%), 18 promises kept / 14 broken / 6 pending, 100 messages** — because the phantom refusal arrived *after* each confirmed mandate had already been scheduled to execute, no falsely-barred entity ever asked for a second mandate inside 45 days, and `trust.update_refusal` turns out to be pending-neutral (no posterior moved). What the fix corrected is the audit trail: **13 → 4 `mandate_refused` events, 10 → 4 entities barred from a future mandate offer, 13 → 4 soft-refusal entries, 154 → 148 bound blocks, 1,099 → 1,084 audit entries.** Full three-part explanation in BUILD_LOG. **The current run is now pinned by `test_integration.py::test_the_45_day_distribution_is_the_number_the_docs_quote`**, so this row cannot go stale silently again. |
| 7 | `v1.0-freeze` tag | ⬜ |
| 8 | re-running arms reproduces identical numbers | ✅ **done 2026-08-27** — `eval/run_arms.py` (three-arm runner: silence / blind-reminder baseline / real Promise Keeper), `tests/test_run_arms.py` asserts byte-identical reproducibility for Arm B and Arm C at seed 42, `metrics.json` committed. See TRACK_BAR.md §1 and BUILD_LOG 2026-08-27. |
| 9 | a friend can run it from README alone | 🟡 the scripts a README would point to are now genuinely verified from a fresh clone (see "Cold start" above); the README itself doesn't exist yet — pending Step 7 of the current gated plan (AFA segmentation section + general repo README) |

---

## Phase A progress log
- 2026-08-26: repo skeleton, git init, `.gitignore`, `.env.example`, spec docs (`CLAUDE.md`, `BUILD.md`, master doc) committed to repo root. venv + Python deps installing. Dashboard scaffolded via `npm create vite`.
- 2026-08-26: `engine/schemas.py` — all 10 BUILD.md §2 contracts + InvoiceCause, smoke-tested.
- 2026-08-26: dataset (60 invoices/12 debtors, 12 carts, 24 threads/93 messages, ground_truth.json) generated via `data/generate.py`, fully schema-validated, 100% ground-truth coverage, deterministic. `sim/clock.py` + `sim/personas.py` + `sim/run.py` written; `python -m sim.run --days 45 --seed 42` produces a 408-event deterministic, schema-valid log. Tagged `personas-frozen`. *(408 was the count on this date; it became 463 when packet P8 fixed the clock bug that had been dropping day-0 work — see the P8 entry below.)*
- 2026-08-26: judgment layer (`engine/judgment/{trust,state_machine,ledger}.py`) — zero LLM, all 8 hard bounds from master doc §3.4 enforced via `check_bounds()`, dispute-from-any-state and 1000-random-sequence termination guarantees verified. 48/48 tests passing.
- 2026-08-26: non-LLM/non-Razorpay Day-6 action-layer slice (`engine/action/{messenger,sentinel,evidence}.py` + `razorpay_client.py` interface stubs) — 15 tests. Perception layer (`engine/perception/{client,triage,extractor,cart_cause}.py` + 5 prompt files + `eval/{triage_eval,extraction_eval}.py`) — fully built, imports cleanly, fails cleanly without a key, 10 tests. Found and fixed a real spec/environment mismatch: `claude-sonnet-4-6` + "temp 0" are both stale against the installed SDK (temperature removed entirely; structured outputs via `output_format=` is the current mechanism) — logged in DECISIONS.md, CLAUDE.md corrected.
- 2026-08-26: `api/main.py` — FastAPI skeleton wired to the real judgment layer, dataset loaded at startup (60 invoices, 2 active reserves). Verified both via `TestClient` and a real `uvicorn api.main:app` process (`curl /health` succeeds). **Phase A complete** — everything BUILD.md schedules for Day 0-2 + Day 5 + the non-LLM/non-Razorpay slice of Day 6, all with real passing tests, no faked results. 76/76 tests passing overall.
- 2026-08-26 (packet P1): perception became provider-pluggable — `engine/perception/providers/` (ABC + registry + file cache at `.cache/perception/`), providers `heuristic` (pure Python, zero deps, default) / `anthropic` (the original Claude path, moved not rewritten) / `oracle` (ground-truth replay, demo beats only, refused by both evals). `extractor.py`/`triage.py`/`cart_cause.py` keep their signatures. Evals gained `--provider` and now write per-provider metrics files. **The whole perception layer runs offline and free for the first time** — Day 3 and Day 4 acceptance criteria are no longer blocked on a key, they are measured (see table above: extraction 97.7% PASS in-sample, triage 71.7% FAIL with the failure diagnosed as a dataset-information limit, not a classifier limit). 169/169 tests passing (+82 new). Two real contradictions found and logged in BUILD_LOG.md.
- 2026-08-26 (later): Razorpay TEST keys received (stored in gitignored `.env` only — never committed). **Day-1 priority #1 DONE:** sandbox verified via live probes, 8/8 green — mandate registration links work for both UPI Autopay and eMandate in test mode (see TRACK_BAR.md §0). Repo pushed to github.com/KernelLex/TrueCommit (master + personas-frozen tag). Build process switched to three-tier orchestration per user instruction (Fable lead / Opus hard packets / Sonnet routine packets).
- 2026-08-26 (packet P2): **the integration runner** — `engine/integration/runner.py`'s `WorldRunner` replaces `sim/run.py`'s simplified internal state model with the real pipeline: real perception (provider-pluggable, cached), real judgment (`ledger.process_event` only — the runner never sets a state and never constructs an `Action`), real action layer (Messenger with rail labels, Sentinel link-open/retry/dead-letter, evidence packets, opt-in Razorpay). Persona tables and touch cadence reused read-only from the frozen `sim/personas.py` + `sim/run.py`. Wired into `api/main.py` as `POST /advance` / `GET /world` / `GET /funnel`, with `ledger` kept as an alias to `runner.ledger` so every pre-existing route and test still passes untouched. 23 new tests (217 total). Three real findings logged in BUILD_LOG.md: a live Razorpay contact-format rejection, the touch cap being per-invoice rather than per-debtor, and `VirtualClock` silently dropping day-0 callbacks.
- 2026-08-26 (packet P8): **the judgment layer was made to match the written law** — the three findings packet P2 logged but was not allowed to fix are now closed. (1) The touch cap is counted **per debtor**, not per invoice: `check_bounds()` takes a `debtor_touches` window as an argument (staying a pure predicate — no hidden state reads) and the Ledger keeps `touches_by_debtor`; carts scope by `customer_id`. Worst rolling per-debtor window over 45 days: **6 → 2**. (2) The gentle nudge is a **real Action**: `_OUTREACH_ACTION` gives TRIAGED/ENGAGED a `message`/`gentle` and ESCALATE_1/2 a `message`/`firm`, and `outreach_sent` re-decides even when the state doesn't move — so every one of the 104 messages a run queues is bounds-checked, audited-before-sent and touch-counted (was 60 messages, only 12 of them plain messages). (3) `VirtualClock.advance()` fires the day it is on before moving off it, so `sim/run.py`'s day-0 beat runs for the first time: **408 → 463 events**, still byte-identical across two runs. **Honest cost, not tuned back: the 45-day distribution fell from 29 KEPT / 19 HANDOFF / 3 DISPUTED, ₹40,62,231 (58.2%) to 23 KEPT / 25 HANDOFF / 3 DISPUTED, ₹25,59,383 (36.6%)** — the old figure was only reachable by over-contacting debtors, so it was never ours to keep. +23 tests from P8 (271 total green at the time of writing, a second packet landing in the same window accounts for the rest); the P2 truth-pinning test flipped from asserting the gap to asserting the law. Two follow-ups flagged in BUILD_LOG for the lead: touch-budget prioritisation (invoices are worked in ID order, so a low-value nudge can block a sibling's mandate offer), and the fact that no entity now reaches ESCALATE_2 in the 45-day run, leaving the voice rung and the legal/merchant-review gate proven only by unit tests.
- 2026-08-26 (packet P7): **the Day-4 extraction gate is green on a real LLM.** The reference-date bug BUILD_LOG flagged on 2026-08-26 is fixed at its root: `engine/perception/assembly.py` is now the ONE place LLM user content is assembled, imported by both `providers/ollama.py` and `providers/anthropic_provider.py` (their duplicated inline copies deleted — the bug existed twice because the code did). Extraction calls carry `Today is <date> (<weekday>)` derived from `message.ts` plus per-line thread dates; triage carries issued/due/days-past-due and a "Today" derived from the latest message on record (never the wall clock — law 6 holds, prompts stay reproducible). `prompts/extract.md` restructured around an explicit decision order with five new few-shots, each traceable to a named miss. Prompt text is now part of the cache fingerprint (`assembly.prompt_fingerprint()` → `identity()`), closing a hole where a prompt edit would NOT have invalidated `.cache/perception/` and the re-measurement could have re-served the old wording's answers. **qwen2.5:7b extraction 77.3% FAIL → 88.6% PASS** (3b 59.1% → 70.5%, still under gate); five-level ladder kept, no fallback taken. The parallel attempt on `triage.md` was measured, found to buy the headline at the gated metric's expense, and **reverted** — both halves are in BUILD_LOG, along with a live CUDA crash that proved the "HTTP error never degrades silently to heuristic" rule earns its keep, and the finding that local-LLM eval numbers carry ±1 message of run-to-run noise.
- 2026-08-26 (packet P11): **the dead function P10 found now has a caller.** `Sentinel.mark_link_opened()` shipped in the Day-6 action layer, was unit-tested, went green, and was never called by `engine/integration/runner.py` — so `link_timed_out()` was true for every instrument ever sent and every dispatched mandate offer soft-refused itself 48 virtual hours later, whatever the debtor had actually said. Two call sites close it: `_dispatch` records `(action_id, kind)` in `_open_instruments` beside the `track_link_sent` it already did, and `_inbound` — the one funnel every debtor message passes through — marks every instrument still inside its window as opened before any 48h check can run. `link_timed_out()` itself and `engine/action/sentinel.py` are **untouched**; genuine silence still soft-refuses. Measured 45-day before/after: distribution and ₹ **unchanged** (21 KEPT / 27 HANDOFF / 3 DISPUTED, ₹23,31,496 / 33.4%), audit trail corrected — **13 → 4 `mandate_refused`, 10 → 4 entities barred by bound #7, 13 → 4 soft refusals, 154 → 148 bound blocks**. The "why didn't recovery go up" answer is measured and in BUILD_LOG, including one part of P10's own diagnosis that measurement disproved (`trust.update_refusal` is pending-neutral, so no trust posterior moved). +5 tests (433 → 438), determinism re-verified byte-identical across two processes.
- 2026-08-26 (post-batch-1): full sandbox demo executed from a clean worktree at commit 85ee9d0 — all eight beats live (169/169 tests, both evals with caveats auto-printed, byte-identical sim replay, full lifecycle + dispute-freeze + cap-block + audit trail via TestClient, Tier-0 zero-touch recovery, two real Razorpay short_urls created, qwen2.5:7b live L3 classification in 1.8s). Beat list + artifacts recorded in PROGRESS.md §"Sandbox demo run". `HANDOVER.md` written for cross-session/account continuity (mechanism, laws, resume procedure, in-flight packet specs).
